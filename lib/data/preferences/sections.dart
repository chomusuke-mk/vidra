import 'package:vidra/constants/app_strings.dart';
import 'package:vidra/data/preferences/preferences_registry.dart';
import 'package:vidra/ui/models/preference_ui_models.dart';

List<PreferenceSection> buildPreferenceSections(Preferences preferences) {
  return [
    PreferenceSection(
      titleKey: AppStringKey.general,
      preferences: [
        preferences.isDarkTheme,
        preferences.language,
        preferences.fontSize,
        preferences.ignoreErrors,
        preferences.abortOnError,
        preferences.useExtractors,
        preferences.liveFromStart,
        preferences.waitForVideo,
      ],
    ),
    PreferenceSection(
      titleKey: AppStringKey.network,
      preferences: [
        preferences.proxy,
        preferences.socketTimeout,
        preferences.sourceAddress,
        preferences.impersonate,
        preferences.foceIPv4,
        preferences.forceIPv6,
        preferences.enableFileUrls,
        preferences.geoVerificationProxy,
        preferences.xff,
        preferences.preferInsecure,
        preferences.addHeaders,
        preferences.cookies,
        preferences.cookiesFromBrowser,
        preferences.username,
        preferences.password,
        preferences.twoFactor,
        preferences.videoPassword,
      ],
    ),
    PreferenceSection(
      titleKey: AppStringKey.video,
      preferences: [
        preferences.videoMultistreams,
        preferences.audioMultistreams,
        preferences.mergeOutputFormat,
        preferences.audioFormat,
        preferences.extractAudio,
        preferences.audioQuality,
        preferences.remuxVideo,
        preferences.embedSubs,
        preferences.embedThumbnail,
        preferences.embedMetadata,
        preferences.embedChapters,
        preferences.embedInfoJson,
        preferences.format,
        preferences.xattrs,
        preferences.fixup,
        preferences.ffmpegLocation,
        preferences.convertThumbnails,
        preferences.writeSubs,
        preferences.writeAutoSubs,
        preferences.subFormat,
        preferences.subLangs,
      ],
    ),
    PreferenceSection(
      titleKey: AppStringKey.downloadSection,
      preferences: [
        preferences.output,
        preferences.paths,
        preferences.downloadArchive,
        preferences.playlist,
        preferences.concurrentFragments,
        preferences.breakOnExisting,
        preferences.skipPlaylistAfterErrors,
        preferences.retries,
        preferences.fileAccessRetries,
        preferences.fragmentRetries,
        preferences.extractorRetries,
        preferences.abortOnUnavailableFragments,
        preferences.keepFragments,
        preferences.batchFile,
        preferences.forceOverwrites,
        preferences.writeThumbnail,
        preferences.sponsorblockMark,
        preferences.limitRate,
      ],
    ),
  ];
}
